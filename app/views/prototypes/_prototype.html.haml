.col-sm-4.col-md-3.proto-content

  .thumbnail
    = link_to image_tag(prototype.set_main_thumbnail), prototype_path(prototype)
    -# = if user_signed_in? && current_user.id == prototype.user_id
    = link_to '編集',"/prototypes/#{prototype.id}/edit", method: :get
    %p
    = link_to '削除', "/prototypes/#{prototype.id}", method: :delete, data: {confirm: '本当に削除してもよろしいですか?'}
    .caption
      %h3
        = prototype.title
      .proto-meta
        .proto-user
          = link_to prototype.user.name, user_path(prototype.user)
        .proto-posted
          = prototype.posted_date

